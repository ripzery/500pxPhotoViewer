<FrameLayout
    android:id="@+id/photoLayout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:dpv="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/ivPhoto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/orange_banana_wallpaper"
        android:scaleType="centerCrop"/>

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="bottom"
        android:background="#80000000">

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="24sp"
            android:maxLines="2"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:layout_marginTop="8dp"
            android:textColor="#fff"
            android:text="Name"
            tools:text="Name"/>

        <TextView
            android:id="@+id/tvDesc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            android:maxLines="2"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:paddingBottom="16dp"
            android:layout_marginTop="8dp"
            android:textColor="#fff"
            android:ellipsize="end"
            android:text="Description"
            tools:text="Description"/>

    </LinearLayout>

    <View
        android:id="@+id/layoutView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="true"
        android:background="?attr/selectableItemBackground" />

    <com.fenjuly.library.ArrowDownloadButton
        android:id="@+id/btnArrowDownload"
        android:layout_gravity="bottom|right"
        android:layout_marginRight="16dp"
        android:layout_marginBottom="16dp"
        android:layout_width="56dp"
        android:layout_height="56dp"/>

    <!--<com.panwrona.downloadprogressbar.library.DownloadProgressBar
        android:id="@+id/downloadProgressBar"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="bottom|right"
        android:layout_marginRight="16dp"
        android:layout_marginBottom="16dp"
        dpv:circleBackgroundColor="@color/colorWhite"
        dpv:progressBackgroundColor="@color/colorWhite"
        dpv:progressColor="@color/colorAccent"
        dpv:drawingColor="@color/colorAccent"
        dpv:resultDuration="4000"
        dpv:progressDuration="2000"
        dpv:circleRadius="24dp"
        dpv:lineWidth="3dp"
        dpv:strokeWidth="3dp"
        dpv:overshootValue="2.5"
        />
-->
</FrameLayout>
